"""End-to-end tests: two-agent localhost demos (Phase 1+).

Two Tesherra principals on different ports, exchanging signed envelopes via
the A2A SDK Adapter. Asserts that the final residue chain on both sides
matches byte-for-byte after a complete interaction.
"""
